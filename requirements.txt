# Python dependencies for rdd-workflow v3.0
# Install: pip install -r requirements.txt
PyYAML>=6.0
jsonschema>=4.0
pytest>=7.0
croniter>=2.0

# Linting & type checking (progressive CI gates)
ruff>=0.1
mypy>=1.8
