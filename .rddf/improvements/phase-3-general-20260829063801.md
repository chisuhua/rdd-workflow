# phase-3-general-20260829063801

**优先级**: P1 | **来源**: from-roadmap (phase-3/general)
**阶段**: phase-3 | **分类**: general
**类型**: functional
**主题**: 流程定制层

## 架构依据

`ADR-0012` (flow customization layer) + `ADR-0020` (incremental skeleton planning) 已定义 plugin loader,但 `_lib/loop/plugin_loader.py` 仅有 stub,无 manifest schema、无 lifecycle hooks、无 reference plugins。**Why now**: 用户为多项目部署 rdd-workflow,每个项目需定制 detector/action。

## 范围

- **In Scope**: plugin manifest JSON schema (`_lib/schemas/plugin_manifest.json`);3 个 reference plugins (rddf-hub-spoke / vscode-extension / github-actions);plugin test harness;plugin lifecycle hooks (load/validate/execute/unload)
- **Out Scope**: plugin marketplace;plugin 签名

## 关键场景

- GIVEN 3rd party 发布 plugin 包含 `plugin.yaml` + `detectors/*.py`
  WHEN rddf 启动
  THEN plugin 被自动加载,detector 注册到 detectors registry
- GIVEN plugin manifest 缺 required field
  WHEN load  THEN 拒绝加载,日志记录但不 crash

## 技术约束

- MUST: plugin 在独立进程/线程运行,主进程不被污染
- MUST NOT: plugin 自动执行网络请求 (除非 manifest 显式声明 `network: true`)

## 验收标准

- 3 reference plugins 端到端测试通过
- plugin manifest schema 测试 (5 个 invalid case reject)
- plugin 隔离性测试 (mock plugin 抛异常不影响主流程)

## Why

plugin loader 仅有 stub,无 manifest schema、无 lifecycle hooks、无 reference plugins。多项目部署 rdd-workflow 时每个项目需定制 detector/action。

## What Changes

- plugin manifest JSON schema (`_lib/schemas/plugin_manifest.json`)
- 3 个 reference plugins (rddf-hub-spoke / vscode-extension / github-actions)
- plugin test harness
- plugin lifecycle hooks (load/validate/execute/unload)

## Capabilities

- MUST: plugin 在独立进程/线程运行,主进程不被污染

## Impact

- MUST NOT: plugin 自动执行网络请求 (除非 manifest 显式声明 `network: true`)

## Acceptance

- [ ] 3 reference plugins 端到端测试通过
- [ ] plugin manifest schema 测试 (5 个 invalid case reject)
- [ ] plugin 隔离性测试 (mock plugin 抛异常不影响主流程)
