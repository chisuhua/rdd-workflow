# phase-1-general-20260829063814

**优先级**: P1 | **来源**: from-roadmap (phase-1/general)
**阶段**: phase-1 | **分类**: general
**类型**: functional
**主题**: 提案生成阶段 — 自动跨仓分析 (A1/A2/A3)

## 架构依据

`ADR-0030` (Hub-Spoke federation) + `ADR-0032` (hub deepening) 已建立 contract sync通道,但 propose阶段 (`skills/propose/`) 仍需手工调用 `cross-repo-protocol` 工具。**Why now**: feat-fix-archive-gaps-v2 涉及跨仓审计,需 propose 阶段自动识别 Hub 边界。

## 范围

- **In Scope**: propose_phase 检测到跨仓提案时自动触发 contract-check;生成 RFC 草稿含 Hub 链接;`.rddf/state/.cross-repo-pending.json` 自动创建条目
- **Out Scope**: 全自动 Hub RFC 提交(留待 #7);Spoke 端合约编辑

## 关键场景

- GIVEN 用户在 propose阶段创建 change涉及 `openspec/specs/api-*/spec.md`
  WHEN guide-plan 调用 propose_phase
  THEN contract-check 自动 run,RFC 草稿含 Hub issue 占位符

## 技术约束

- MUST: 仅 MCP 工具调用,fallback REST (per cross-repo-protocol v1.2+)
- MUST NOT: 在 propose阶段直接 file Hub issue (等审批后由 #7 负责)
- SHOULD: trace 日志写入 `.rddf/state/.cross-repo-deps-cache.json`

## 验收标准

- 3 个测试用例:纯本地提案无 RFC 占位 / 跨仓提案含占位 / 跨仓提案缓存命中跳过重新生成
- 端到端: propose → design → plan 全程0 手工 cross-repo 调用

## Why

propose 阶段 (`skills/propose/`) 仍需手工调用 cross-repo-protocol 工具识别 Hub 边界,feat-fix-archive-gaps-v2 涉及跨仓审计时反复需要手工触发。

## What Changes

- propose_phase 检测到跨仓提案时自动触发 contract-check
- RFC 草稿自动含 Hub 链接占位符
- `.rddf/state/.cross-repo-pending.json` 自动创建条目

## Capabilities

- MUST: 仅 MCP 工具调用,fallback REST (per cross-repo-protocol v1.2+)

## Impact

- MUST NOT: 在 propose 阶段直接 file Hub issue (留待审批后由其他提案负责)

## Acceptance

- [ ] 3 个测试用例:纯本地提案无 RFC 占位 / 跨仓提案含占位 / 缓存命中跳过重新生成
- [ ] 端到端: propose → design → plan 全程 0 手工 cross-repo 调用
- [ ] `.cross-repo-pending.json` schema 测试覆盖 (含正常/异常/边界 3 路径)
