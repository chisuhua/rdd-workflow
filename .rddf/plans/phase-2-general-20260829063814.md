# phase-2-general-20260829063814 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use skill_use("execute") to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** rfc_interview 流程补齐 5 种 ambiguity detection + 断点续传 + proposal 拆分建议,接入 guide-design Phase 3 默认前置。

**Architecture:** 新增 `_lib/rfc_ambiguity.py` 做静态分析(无 LLM),返回 (Ambiguity, suggestion, severity)。`_lib/cli/rfc_interview_state.py` 持久化断点状态。`rfc_interview.sh` 触发前置检查 → 调用 Python 检测 → 状态持久化 → 用户决策。

**Tech Stack:** Python 3.11+ / pytest / re / JSON / bash (rfc_interview.sh 已存在)。

---

## File Structure

### Production Code

| File | Responsibility |
|---|---|
| `_lib/rfc_ambiguity.py` (新增) | `detect_ambiguity(proposal_path) -> list[Ambiguity]` — 5 种检测 |
| `_lib/rfc_interview_state.py` (新增) | `load_state(name)` / `save_state(name, state)` / `clear_state(name)` — 断点续传 |
| `skills/guide-design/scripts/rfc_interview.sh` | 增加 `precheck` 子模式 + 检测调用 |

### Tests

| File | Responsibility |
|---|---|
| `tests/unit/test_rfc_ambiguity.py` (新增) | 5 种 ambiguity 检测 |
| `tests/integration/test_rfc_interview_resume.py` (新增) | 中断恢复场景 |

---

### Task 1: ambiguity 检测 (5 acceptance cases)

**Files:**
- Create: `_lib/rfc_ambiguity.py`
- Test: `tests/unit/test_rfc_ambiguity.py`

- [ ] **Step 1: Write the failing test**

```python
"""tests/unit/test_rfc_ambiguity.py"""
"""RFC ambiguity detection (5 acceptance cases per phase-2-general-20260829063814)."""
from __future__ import annotations
from pathlib import Path

from _lib.rfc_ambiguity import detect_ambiguity, Ambiguity


def _write(tmp_path: Path, body: str) -> Path:
    p = tmp_path / "proposal.md"
    p.write_text(body, encoding="utf-8")
    return p


def test_missing_acceptance_section(tmp_path):
    body = "# Test\n\n## Why\nTBD\n\n## What Changes\nTBD\n"
    out = detect_ambiguity(_write(tmp_path, body))
    kinds = {a.kind for a in out}
    assert "missing_acceptance" in kinds


def test_scope_exceeds_threshold(tmp_path):
    """In Scope with >5 files → suggest split."""
    files = "\n".join(f"- path/to/file_{i}.py" for i in range(8))
    body = f"# Test\n\n## Why\nx\n\n## What Changes\nIn Scope:\n{files}\n\n## Acceptance\n- [ ] A\n- [ ] B\n- [ ] C\n"
    out = detect_ambiguity(_write(tmp_path, body))
    kinds = {a.kind for a in out}
    assert "scope_overflow" in kinds


def test_multi_stakeholder_indicates_federation(tmp_path):
    """Cross-repo keywords → 'multi_stakeholder' flag."""
    body = (
        "# Test\n\n## Why\nTouches api-auth and Hub cross-repo.\n\n"
        "## What Changes\nIn Scope: api-foo, api-bar\n\n## Acceptance\n- [ ] A\n- [ ] B\n- [ ] C\n"
    )
    out = detect_ambiguity(_write(tmp_path, body))
    kinds = {a.kind for a in out}
    assert "multi_stakeholder" in kinds


def test_self_contradiction_in_scope(tmp_path):
    """In Scope AND Out of Scope both mention the same module → contradiction."""
    body = (
        "# Test\n\n## Why\nx\n\n"
        "## What Changes\nIn Scope: api-foo, api-bar\n\nOut of Scope: api-foo\n\n"
        "## Acceptance\n- [ ] A\n- [ ] B\n- [ ] C\n"
    )
    out = detect_ambiguity(_write(tmp_path, body))
    kinds = {a.kind for a in out}
    assert "contradiction" in kinds


def test_vague_language_detected(tmp_path):
    """Hedge words + no Acceptance items → vague."""
    body = (
        "# Test\n\n## Why\nmaybe this should probably fix things.\n\n"
        "## What Changes\nIn Scope: api-foo\n\n## Acceptance\n"
    )
    out = detect_ambiguity(_write(tmp_path, body))
    kinds = {a.kind for a in out}
    assert "vague" in kinds
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/unit/test_rfc_ambiguity.py -v`
Expected: FAIL (5 errors) — `_lib.rfc_ambiguity` 不存在。

- [ ] **Step 3: Write minimal implementation**

创建 `_lib/rfc_ambiguity.py`:

```python
"""Static ambiguity detection for RFC proposals (no LLM, fast, deterministic).

Detects 5 ambiguity kinds per phase-2-general-20260829063814 acceptance:
  - missing_acceptance: no `## Acceptance` section or no `- [ ]` items
  - scope_overflow: >5 items in In Scope block
  - multi_stakeholder: cross-repo / Hub / api-*/cross-* hints
  - contradiction: items appearing in both In Scope and Out of Scope
  - vague: hedge words (maybe/should/probably) AND missing acceptance

Each Ambiguity has: kind, severity, suggestion. Non-blocking — rfc_interview.sh
displays them; user decides y/n/d/s.
"""
from __future__ import annotations
import re
from dataclasses import dataclass
from pathlib import Path

ACCEPTANCE_HDR_RE = re.compile(r"^## Acceptance\b", re.MULTILINE)
ACCEPTANCE_CHECKBOX_RE = re.compile(r"^\s*-\s*\[\s*\]", re.MULTILINE)
IN_SCOPE_HDR_RE = re.compile(r"^In Scope\s*:?\s*$", re.MULTILINE)
OUT_SCOPE_HDR_RE = re.compile(r"^Out\s*of\s*Scope\s*:?\s*$", re.MULTILINE | re.IGNORECASE)
LIST_ITEM_RE = re.compile(r"^\s*-\s+(.+?)\s*$", re.MULTILINE)
CROSS_REPO_HINT_RE = re.compile(r"\b(cross[- ]?repo|Hub|api-|cross-)\b", re.IGNORECASE)
HEDGE_WORDS_RE = re.compile(r"\b(maybe|probably|should\s+(probably|maybe)|might\s+be)\b", re.IGNORECASE)

SCOPE_THRESHOLD = 5


@dataclass
class Ambiguity:
    kind: str
    severity: str  # "block" | "warn" | "info"
    suggestion: str


def _extract_block(text: str, header_re: re.Pattern, stop_re: re.Pattern) -> str:
    """Extract the body under `header_re` until next markdown header or EOF."""
    m = header_re.search(text)
    if not m:
        return ""
    start = m.end()
    rest = text[start:]
    stop = re.search(r"^##\s", rest, re.MULTILINE)
    return rest[: stop.start()] if stop else rest


def _list_items(block: str) -> list[str]:
    return [m.group(1).strip() for m in LIST_ITEM_RE.finditer(block)]


def _first_n_words(text: str, n: int = 100) -> str:
    words = re.findall(r"\b\w+\b", text)
    return " ".join(words[:n]).lower()


def detect_ambiguity(proposal_path) -> list:
    path = Path(proposal_path)
    if not path.exists():
        return [Ambiguity("missing_proposal", "block", f"proposal.md not found: {proposal_path}")]
    text = path.read_text(encoding="utf-8")

    out: list = []

    # missing_acceptance
    has_acc = bool(ACCEPTANCE_HDR_RE.search(text))
    has_checkbox = bool(ACCEPTANCE_CHECKBOX_RE.search(text))
    if not has_acc or not has_checkbox:
        out.append(Ambiguity(
            "missing_acceptance", "warn",
            "Add ## Acceptance section with >=3 `- [ ]` items.",
        ))

    in_scope_block = _extract_block(text, IN_SCOPE_HDR_RE, re.compile(r"^##\s", re.MULTILINE))
    in_scope_items = _list_items(in_scope_block)

    # scope_overflow
    if len(in_scope_items) > SCOPE_THRESHOLD:
        out.append(Ambiguity(
            "scope_overflow", "warn",
            f"In Scope has {len(in_scope_items)} items (>5). Consider splitting into sub-proposals.",
        ))

    out_scope_block = _extract_block(text, OUT_SCOPE_HDR_RE, re.compile(r"^##\s", re.MULTILINE))
    out_scope_items = set(_list_items(out_scope_block))

    # contradiction (item appears in both)
    both = set(in_scope_items) & out_scope_items
    if both:
        out.append(Ambiguity(
            "contradiction", "block",
            f"Items appear in both In Scope and Out of Scope: {sorted(both)}.",
        ))

    # multi_stakeholder
    if CROSS_REPO_HINT_RE.search(text) and ("api-" in text or "cross-" in text):
        out.append(Ambiguity(
            "multi_stakeholder", "info",
            "Detected cross-repo keywords; Hub RFC may be required (per ADR-0031).",
        ))

    # vague (hedge words + missing acceptance)
    head = _first_n_words(text, 100)
    if HEDGE_WORDS_RE.search(head) and not has_checkbox:
        out.append(Ambiguity(
            "vague", "warn",
            "Hedge words ('maybe', 'probably') combined with missing acceptance items.",
        ))

    return out
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/unit/test_rfc_ambiguity.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Defer commit**

按仓库约定，execute 阶段不逐任务 commit。

---

### Task 2: 断点续传 (recovery test)

**Files:**
- Create: `_lib/rfc_interview_state.py`
- Test: `tests/integration/test_rfc_interview_resume.py`

- [ ] **Step 1: Write the failing test**

```python
"""tests/integration/test_rfc_interview_resume.py"""
"""RFC interview state persistence + resume after interruption."""
from __future__ import annotations
import json
from pathlib import Path

import pytest

from _lib.rfc_interview_state import (
    load_state,
    save_state,
    clear_state,
    STATE_PATH_TEMPLATE,
)


def test_state_path_uses_rfc_dot_prefix(tmp_path, monkeypatch):
    """State file lives at .rddf/state/.rfc-interview-<name>.json (gitignored)."""
    monkeypatch.setenv("RDDF_STATE_DIR", str(tmp_path))
    path = STATE_PATH_TEMPLATE.format(name="my-proposal")
    assert path == f"{tmp_path}/.rfc-interview-my-proposal.json"


def test_save_load_roundtrip(tmp_path, monkeypatch):
    monkeypatch.setenv("RDDF_STATE_DIR", str(tmp_path))
    state = {"name": "my-proposal", "step": 2, "draft": {"title": "RFC"}}
    save_state("my-proposal", state)
    loaded = load_state("my-proposal")
    assert loaded == state


def test_resume_after_state_deletion(tmp_path, monkeypatch):
    """Acceptance: 删除 .json 后重跑 — load_state returns None (fresh start)."""
    monkeypatch.setenv("RDDF_STATE_DIR", str(tmp_path))
    save_state("x", {"step": 1})
    assert load_state("x") == {"step": 1}
    clear_state("x")
    assert load_state("x") is None


def test_load_state_missing_file_returns_none(tmp_path, monkeypatch):
    monkeypatch.setenv("RDDF_STATE_DIR", str(tmp_path))
    assert load_state("never-saved") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/integration/test_rfc_interview_resume.py -v`
Expected: FAIL (4 errors) — `_lib.rfc_interview_state` 不存在。

- [ ] **Step 3: Write minimal implementation**

创建 `_lib/rfc_interview_state.py`:

```python
"""RFC interview checkpoint state (resume support, per phase-2-general-20260829063814 acceptance).

State file: .rddf/state/.rfc-interview-<name>.json (gitignored).
Override path via RDDF_STATE_DIR (for tests).
"""
from __future__ import annotations
import json
import os
from pathlib import Path
from typing import Optional

DEFAULT_STATE_DIR = ".rddf/state"
STATE_PATH_TEMPLATE = "{state_dir}/.rfc-interview-{name}.json"


def _state_path(name: str) -> Path:
    state_dir = os.environ.get("RDDF_STATE_DIR", DEFAULT_STATE_DIR)
    return Path(STATE_PATH_TEMPLATE.format(state_dir=state_dir, name=name))


def load_state(name: str) -> Optional[dict]:
    path = _state_path(name)
    if not path.exists():
        return None
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError:
        return None


def save_state(name: str, state: dict) -> None:
    path = _state_path(name)
    path.parent.mkdir(parents=True, exist_ok=True)
    path.write_text(json.dumps(state, indent=2, ensure_ascii=False), encoding="utf-8")


def clear_state(name: str) -> None:
    path = _state_path(name)
    if path.exists():
        path.unlink()
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/integration/test_rfc_interview_resume.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Defer commit**

按仓库约定，execute 阶段不逐任务 commit。

---

### Task 3: rfc_interview.sh 接入 precheck

**Files:**
- Modify: `skills/guide-design/scripts/rfc_interview.sh`

- [ ] **Step 1: 在 rfc_interview.sh 顶部 NAME 校验后,加入 precheck 调用**

```bash
# Precheck: 5-kind ambiguity detection (per phase-2-general-20260829063814)
PROJECT_ROOT="${PROJECT_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}"
PROPOSAL_MD="$PROJECT_ROOT/openspec/changes/$NAME/proposal.md"
if [ -f "$PROPOSAL_MD" ]; then
    python3 - "$PROPOSAL_MD" <<'PYEOF'
import sys
sys.path.insert(0, "$PROJECT_ROOT")
from _lib.rfc_ambiguity import detect_ambiguity
from _lib.rfc_interview_state import save_state, load_state
prop = sys.argv[1]
items = detect_ambiguity(prop)
if items:
    print(f"\n⚠️  RFC ambiguity check ({len(items)} finding(s)):")
    for a in items:
        print(f"  [{a.severity}] {a.kind}: {a.suggestion}")
    save_state("$NAME", {"ambiguities": [vars(a) for a in items]})
else:
    print("✅ RFC precheck: no ambiguities detected.")
    save_state("$NAME", {"ambiguities": []})
PYEOF
fi
```

> 实现里需要把 `$NAME` 嵌入 heredoc 会失效。改为 bash 变量替换: heredoc 不带引号前缀 (`<<PYEOF` not `<<'PYEOF'`) 让 `$NAME` 被替换。

- [ ] **Step 2: 验证 bash 语法 + smoke 测试**

```bash
bash -n skills/guide-design/scripts/rfc_interview.sh  # 语法检查
```

Expected: exit 0。

- [ ] **Step 3: 更新 tasks.md 勾选 + commit**

按仓库约定，execute 阶段不逐任务 commit。

---

## Self-Review Checklist

1. **Spec 覆盖**:
   - [x] Acceptance 1 (5 测试用例) → Task 1
   - [x] Acceptance 2 (中断恢复) → Task 2
   - [x] Acceptance 3 (3 backlog 提案从延迟→活跃) → 这是 ops 工作流,非代码;在 archive 阶段验证。
   - [x] Capability MUST (state 持久化) → Task 2
   - [x] Capability SHOULD (与 question 协同) → plan 中未实现 bash 调用 question;但 detector 输出适合直接 display 给用户,后续可升级。
   - [x] Impact MUST NOT (不强制修改) → detector 只输出 suggestion,user 决定。
2. **占位符扫描**: 无 TBD。
3. **类型一致性**: `Ambiguity(kind, severity, suggestion)` 在两个模块统一。

## 执行交接

- 推荐当前 session 直接按 `skill_use("execute")` 执行本 plan。
- 完工后用 `sed` 更新 `openspec/changes/phase-2-general-20260829063814/tasks.md` 勾选。