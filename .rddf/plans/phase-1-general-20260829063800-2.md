# phase-1-general-20260829063800-2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use skill_use("execute") to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 rdd-workflow 4 个 scheduler 补齐测试覆盖（cron/fs-watcher/git-hook/webhook），新增 `install.sh --git-hooks` 选项与 webhook HMAC 验签，提供 `rddf scheduler status` CLI。锁定 3 条验收：12 测试用例 / install.sh hooks 生效 / HMAC 4 异常路径。

**Architecture:** 复用 `_lib/schedulers/` 4 个既有模块（cron_scheduler.py / fs_watcher.py / git_hook.py / webhook_receiver.py），补充测试缺口、给 webhook_receiver 加 HMAC-SHA256 验签（新常量 + 可选 `secret` 参数）、给 install.sh 加 `--git-hooks` 分支、新增 `_lib/cli/scheduler_cmd.py` CLI 子命令。

**Tech Stack:** Python 3.11+ / pytest / hmac / hashlib / http.server / bash (install.sh) / bats。

---

## File Structure

### Production Code

| File | Responsibility |
|---|---|
| `_lib/schedulers/webhook_receiver.py` | 新增 `secret` 可选参数 + HMAC-SHA256 验签（`X-Hub-Signature` header） |
| `install.sh` | 新增 `--git-hooks` 选项：安装 post-commit hook 到 `.git/hooks/post-commit` |
| `_lib/cli/scheduler_cmd.py` | 新增 `rddf scheduler status` 子命令（一次性查询 4 个 scheduler 状态） |

### Tests

| File | Responsibility |
|---|---|
| `tests/unit/test_webhook_hmac.py` (新增) | HMAC 验签：正确签名通过 / 错误签名拒绝 / 缺失 header / 时间戳过期 / secret 不匹配（4+ 异常路径） |
| `tests/unit/test_cron_scheduler_more.py` (新增) | cron 补充测试：无效表达式 / next_fire_time / schedule 幂等 / stop 释放线程 |
| `tests/unit/test_git_hook_more.py` (新增) | git-hook 补充：`_current_head` / `_list_branches` / `_list_tags` / 新 commit 触发 event |
| `tests/bats/test_install_git_hooks.bats` (新增) | install.sh `--git-hooks` 安装后 `.git/hooks/post-commit` 存在且可执行 |
| `tests/unit/test_scheduler_cmd.py` (新增) | `rddf scheduler status` 子命令解析 + 输出 |

---

### Task 1: webhook HMAC-SHA256 验签

**Files:**
- Modify: `_lib/schedulers/webhook_receiver.py`
- Test: `tests/unit/test_webhook_hmac.py`

- [ ] **Step 1: Write the failing test**

```python
"""tests/unit/test_webhook_hmac.py"""
"""HMAC-SHA256 verification for webhook receiver (acceptance: 4+ exceptions)."""
from __future__ import annotations

import hashlib
import hmac
import json

import pytest

from skills._lib.schedulers.webhook_receiver import (
    WebhookReceiver,
    _WebhookHandler,
    verify_signature,
)


class TestHmacVerification:
    """Unit tests for HMAC signature verification (no server needed)."""

    def _sign(self, secret: str, payload: bytes) -> str:
        return hmac.new(secret.encode(), payload, hashlib.sha256).hexdigest()

    def test_valid_signature_passes(self):
        secret = "s3cret"
        payload = b'{"event":"push"}'
        sig = self._sign(secret, payload)
        assert verify_signature(secret, sig, payload) is True

    def test_invalid_signature_rejected(self):
        secret = "s3cret"
        payload = b'{"event":"push"}'
        wrong = "0" * 64
        assert verify_signature(secret, wrong, payload) is False

    def test_missing_signature_rejected(self):
        secret = "s3cret"
        payload = b'{"event":"push"}'
        assert verify_signature(secret, "", payload) is False

    def test_wrong_secret_rejected(self):
        """A signature produced with a different secret must fail."""
        payload = b'{"event":"push"}'
        good_sig = self._sign("other-secret", payload)
        assert verify_signature("s3cret", good_sig, payload) is False

    def test_tampered_payload_rejected(self):
        """Signature valid for original body must fail for modified body."""
        secret = "s3cret"
        original = b'{"event":"push"}'
        tampered = b'{"event":"delete"}'
        sig = self._sign(secret, original)
        assert verify_signature(secret, sig, tampered) is False
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/unit/test_webhook_hmac.py -v`
Expected: FAIL (5 errors) — `verify_signature` 未定义。

- [ ] **Step 3: Write minimal implementation**

在 `_lib/schedulers/webhook_receiver.py` 顶部（`import json` 后）加：

```python
import hashlib
import hmac
from typing import Optional


def verify_signature(secret: str, signature: str, payload: bytes,
                     digestmod=hashlib.sha256) -> bool:
    """Verify HMAC-SHA256 signature (format: 'sha256=<hex>').

    An empty/missing signature is always rejected (fail closed).
    """
    if not secret or not signature or not signature.startswith("sha256="):
        return False
    expected = hmac.new(secret.encode(), payload, digestmod).hexdigest()
    provided = signature[len("sha256="):]
    return hmac.compare_digest(expected, provided)
```

在 `WebhookReceiver.__init__` 加可选参数，并把 secret 传递给 handler：

```python
    def __init__(self, event_queue, on_fire, host="127.0.0.1", port=9090,
                 secret: Optional[str] = None):
        ...
        self.secret = secret
        _WebhookHandler.queue = event_queue
        _WebhookHandler.on_fire = on_fire
        _WebhookHandler.secret = secret
```

在 `_WebhookHandler` 类级加 `secret: Optional[str] = None`，并在 `do_POST` 内验签：

```python
    def do_POST(self):
        length = int(self.headers.get("Content-Length", 0))
        body = self.rfile.read(length) if length else b"{}"
        raw_body = body

        # HMAC verification when a secret is configured (fail closed).
        if self.secret:
            provided = self.headers.get("X-Hub-Signature", "")
            if not verify_signature(self.secret, provided, raw_body):
                self.send_response(401)
                self.send_header("Content-Type", "application/json")
                self.end_headers()
                self.wfile.write(b'{"ok":false,"error":"invalid-signature"}')
                return
        try:
            payload = json.loads(raw_body.decode("utf-8")) if raw_body else {}
        except json.JSONDecodeError:
            payload = {"raw": raw_body.decode("utf-8")}
        ...
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/unit/test_webhook_hmac.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Defer commit**

按仓库约定，execute 阶段不逐任务 commit。

---

### Task 2: scheduler 幂等 + 状态 CLI

**Files:**
- Modify: `_lib/schedulers/cron_scheduler.py`（若有幂等缺口补 LOCK）
- Create: `_lib/cli/scheduler_cmd.py`
- Test: `tests/unit/test_cron_scheduler_more.py` + `tests/unit/test_scheduler_cmd.py`

- [ ] **Step 1: Write the failing test**

```python
"""tests/unit/test_cron_scheduler_more.py"""
"""Additional CronScheduler coverage: edge cases + idempotency."""
from __future__ import annotations

import datetime

import pytest

from skills._lib.schedulers.cron_scheduler import (
    CronScheduler, CronExpressionError, next_fire_time, validate_cron,
)
from skills._lib.triggers import Trigger, TriggerManager


class TestCronValidation:
    def test_valid_cron_ok(self):
        assert validate_cron("*/5 * * * *") is True
        assert validate_cron("0 8 * * 1") is True

    def test_invalid_cron_rejected(self):
        assert validate_cron("not-a-cron") is False
        assert validate_cron("") is False
        assert validate_cron("61 * * * *") is False  # minute out of range

    def test_next_fire_time_after(self):
        base = datetime.datetime(2026, 1, 1, 0, 0, 0)
        nxt = next_fire_time("*/15 * * * *", after=base)
        assert nxt >= base
        assert (nxt - base).total_seconds() <= 900

    def test_next_fire_invalid_raises(self):
        with pytest.raises(CronExpressionError):
            next_fire_time("bad", after=datetime.datetime.now())


class TestCronSchedulerIdempotency:
    def test_schedule_same_trigger_twice_noop(self):
        mgr = TriggerManager()
        mangled = []  # collect on_fire calls
        sched = CronScheduler(mgr, on_fire=lambda tid: mangled.append(tid))
        trg = Trigger(id="t1", type="cron", config={"expression": "0 0 1 1 *"})
        mgr.add(trg)
        assert sched.schedule(trg) is True
        assert sched.schedule(trg) is False  # already scheduled (idempotent)
        sched.stop()

    def test_schedule_all_counts(self):
        mgr = TriggerManager()
        trg1 = Trigger(id="a", type="cron", config={"expression": "0 0 1 1 *"})
        trg2 = Trigger(id="b", type="cron", config={"expression": "bad"})
        mgr.add(trg1)
        mgr.add(trg2)
        sched = CronScheduler(mgr, on_fire=lambda tid: None)
        assert sched.schedule_all() == 1  # invalid expression skipped
        sched.stop()
```

```python
"""tests/unit/test_scheduler_cmd.py"""
"""Verify `rddf scheduler status` subcommand parsing + output."""
from pathlib import Path
from unittest.mock import patch


class TestSchedulerCmd:
    def test_subcommand_dispatches(self):
        import sys
        sys.path.insert(0, str(Path.cwd()))
        from _lib.cli.scheduler_cmd import cmd_scheduler

        with patch("_lib.cli.scheduler_cmd._status", return_value=0) as mock:
            rc = cmd_scheduler(["status"])
            assert rc == 0
            mock.assert_called_once_with()

    def test_status_no_args_returns_2(self):
        import sys
        sys.path.insert(0, str(Path.cwd()))
        from _lib.cli.scheduler_cmd import cmd_scheduler
        assert cmd_scheduler([]) == 2

    def test_status_prints_4_schedulers(self, capsys):
        import sys
        sys.path.insert(0, str(Path.cwd()))
        from _lib.cli.scheduler_cmd import _status
        rc = _status()
        out = capsys.readouterr().out
        for name in ("cron", "fs-watcher", "git-hook", "webhook"):
            assert name in out
        assert rc == 0
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m pytest tests/unit/test_cron_scheduler_more.py tests/unit/test_scheduler_cmd.py -v`
Expected: FAIL (5 errors) — `_lib/cli/scheduler_cmd.py` 不存在；若 `schedule` 幂等已实现则部分通过。

- [ ] **Step 3: Write minimal implementation**

**新增 `_lib/cli/scheduler_cmd.py`**（Oracle C1 安全，无 shell 插值）：

```python
"""``rddf scheduler`` — one-shot status of the 4 built-in schedulers."""
from __future__ import annotations

import sys
from typing import Optional


def _status() -> int:
    """Print one-line status per scheduler (best-effort, no threads started)."""
    import importlib.util

    def _available(module: str) -> bool:
        try:
            return importlib.util.find_spec(module) is not None
        except (ImportError, ValueError):
            return False

    schedulers = [
        ("cron", "skills._lib.schedulers.cron_scheduler"),
        ("fs-watcher", "skills._lib.schedulers.fs_watcher"),
        ("git-hook", "skills._lib.schedulers.git_hook"),
        ("webhook", "skills._lib.schedulers.webhook_receiver"),
    ]
    print("📊 Scheduler status (modules installed, importable)")
    print(f"{'SCHEDULER':<12} {'MODULE':<45} STATE")
    print("-" * 70)
    for name, mod in schedulers:
        ok = _available(mod)
        state = "✅ importable" if ok else "❌ missing"
        print(f"{name:<12} {mod:<45} {state}")
    return 0


def cmd_scheduler(args) -> int:
    """Handle ``rddf scheduler [status]``."""
    if not args or args[0] in ("-h", "--help", "help"):
        print("usage: rddf scheduler [status]")
        print()
        print("sub-commands:")
        print("  status    One-shot status of 4 built-in schedulers (default)")
        return 0
    sub = args[0]
    if sub == "status":
        return _status()
    print(f"❌ scheduler: unknown sub-command {sub!r}", file=sys.stderr)
    print("   usage: rddf scheduler [status]", file=sys.stderr)
    return 2
```

并在 `_lib/cli/__init__.py`（或 rddf 入口引用的 subcommand registry）注册 `scheduler` → `cmd_scheduler`（若 registry 是 dict/map，加 `"scheduler": cmd_scheduler`）。

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m pytest tests/unit/test_cron_scheduler_more.py tests/unit/test_scheduler_cmd.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Defer commit**

按仓库约定，execute 阶段不逐任务 commit。

---

### Task 3: install.sh --git-hooks + bats 测试

**Files:**
- Modify: `install.sh`
- Test: `tests/bats/test_install_git_hooks.bats`

- [ ] **Step 1: Write the failing test**

```bash
#!/usr/bin/env bats
# tests/bats/test_install_git_hooks.bats
load test_helper

@test "install.sh: --git-hooks installs post-commit hook" {
    tmp=$(mktemp -d)
    cd "$tmp"
    git init -q
    run bash "$BATS_TEST_DIRNAME/../../install.sh" --git-hooks --project-root "$tmp"
    assert_success
    assert_file_exists "$tmp/.git/hooks/post-commit"
    assert_file_executable "$tmp/.git/hooks/post-commit"
}
```

- [ ] **Step 2: Run test to verify it fails**

Run: `bats tests/bats/test_install_git_hooks.bats`
Expected: FAIL — `install.sh` 无 `--git-hooks` 分支（usage 错误）。

- [ ] **Step 3: Write minimal implementation**

在 `install.sh` 的 main case 分支（`--global|-g` / `--help|-h` 附近）加：

```bash
    --git-hooks)
        # Install repo-local post-commit hook that syncs iteration.json
        # (ADR-0027 / add-archive-post-commit-hook-and-force-flag context).
        PROJECT_ROOT="${2:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}"
        HOOK="$PROJECT_ROOT/.git/hooks/post-commit"
        mkdir -p "$(dirname "$HOOK")"
        cat > "$HOOK" <<'HOOK_EOF'
#!/bin/sh
# rdd-workflow post-commit hook: refresh iteration view after any commit.
if command -v rddf >/dev/null 2>&1; then
    rddf iteration sync >/dev/null 2>&1 || true
fi
HOOK_EOF
        chmod +x "$HOOK"
        echo "✅ post-commit hook installed: $HOOK"
        exit 0
        ;;
```

> 注意：`--project-root` 与 `$2` 的优先级以实际 install.sh 参数 API 为准；按仓库既有 `--spoke-init --tools` 的模式，若 `$2` 已是 flag 则跳过（容错）。

- [ ] **Step 4: Run test to verify it passes**

Run: `bats tests/bats/test_install_git_hooks.bats`
Expected: PASS (1 passed)

- [ ] **Step 5: Defer commit**

按仓库约定，execute 阶段不逐任务 commit。

---

## Self-Review Checklist

1. **Spec 覆盖**:
   - [x] Acceptance 1 (12 测试用例) → Task 1 (5 HMAC) + Task 2 (5 cron/cmd) + 已有 test_event_schedulers (fs/git/webhook 基础)
   - [x] Acceptance 2 (install.sh --git-hooks) → Task 3
   - [x] Acceptance 3 (HMAC 4 异常路径) → Task 1 (invalid/missing/wrong-secret/tampered)
   - [x] Capability MUST (scheduler 幂等) → Task 2 (schedule 幂等测试)
   - [x] Capability SHOULD (rddf scheduler status) → Task 2 (scheduler_cmd.py)
   - [x] Impact MUST NOT (新依赖) → 仅用 stdlib hmac/hashlib
2. **占位符扫描**: 无 TBD/TODO；test 全部含实际断言。
3. **类型一致性**: `verify_signature(secret, signature, payload)` 签名在一个文件内统一；`cmd_scheduler(args)` 返回 int；install.sh 的 HOOK heredoc 用引用型 `'HOOK_EOF'`（无 shell 插值，Oracle C1 安全）。

## 执行交接

- 推荐当前 session 直接按 `skill_use("execute")` 执行本 plan。
- 完工后用 `sed` 更新 `openspec/changes/phase-1-general-20260829063800-2/tasks.md` 勾选。