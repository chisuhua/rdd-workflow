# fix-adr-0031-safety-gate-substantiation — Implementation Plan

> Generated by guide-ship Phase 1
> Change: fix-adr-0031-safety-gate-substantiation
> Mode: worktree

## Goal

Implement fix-adr-0031-safety-gate-substantiation per proposal AC + design decisions.

## Architecture

### Decision 1: Approach from proposal 关键场景

The proposal's 5 key scenarios drive the implementation:

## Tech Stack

- bash (approve_proposal.sh, gate scripts)
- Python 3.11+ (_lib modules, cross_repo_audit.py, iteration/store.py)
- bats (integration tests)
- pytest (unit tests)
- openspec CLI v1.4.1

## Tasks

### Task 1: `tests/integration/test_strict_human_approval.bats` 新增 5 个 case，总计 9 个 case 全绿

**Files:**
- Test: tests/integration/test_*.bats

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `tests/integration/test_strict_human_approval.bats` 新增 5 个 c...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 2: **fail-open 防御**: 测试场景 1 中 `--auto-accept` 在首次调用时必须 exit 3（不依赖 change dir 存在）

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **fail-open 防御**: 测试场景 1 中 `--auto-accept` 在首次调用时必须 exit 3（不...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 3: **username 强制**: 测试场景 2 中空 stdin / 30s timeout 必须 exit 4

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **username 强制**: 测试场景 2 中空 stdin / 30s timeout 必须 exit 4...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 4: **env var 升级**: 测试场景 3 中 `RDDF_REQUIRE_HUB_APPROVAL=yes` + username + hub approved label → accept；缺 label → exit 5

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **env var 升级**: 测试场景 3 中 `RDDF_REQUIRE_HUB_APPROVAL=yes` + u...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 5: **hub re-fetch**: 测试场景 4 中 issue closed → exit 6；network error → exit 0 + warning

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **hub re-fetch**: 测试场景 4 中 issue closed → exit 6；network err...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 6: **audit 写入**: 测试场景 5 中 accept 后 `.cross-repo-audit.jsonl` 含新行（含 actor / hub_state / hub_labels / decision）

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **audit 写入**: 测试场景 5 中 accept 后 `.cross-repo-audit.jsonl` 含新...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 7: **既有回归**: `tests/unit/test_cross_repo_audit.py` 全绿, `tests/unit/test_approve_proposal*.py` 全绿

**Files:**
- Test: tests/unit/test_*.py
- Modify: skills/guide-design/scripts/approve_proposal.sh
- Modify: skills/_lib/cross_repo_audit.py

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **既有回归**: `tests/unit/test_cross_repo_audit.py` 全绿, `tests/u...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 8: **全量回归**: `./test.sh --full --regression` 通过（无新增失败）

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **全量回归**: `./test.sh --full --regression` 通过（无新增失败）...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 9: **ADR-0031 修订**: 状态 `待定 → 已采纳`, §实现细节与本次实际代码一致

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **ADR-0031 修订**: 状态 `待定 → 已采纳`, §实现细节与本次实际代码一致...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 10: **AC 文档同步**: `2026-08-16-add-strict-human-approval-for-cross-repo-changes` 的 AC 清单据实修正, 不再声称 5/5 实现

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **AC 文档同步**: `2026-08-16-add-strict-human-approval-for-cross...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 11: **Audit trail**: `.rddf/state/.cross-repo-audit.jsonl` 经端到端测试后**非空**（验证 dead-code 修复）

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **Audit trail**: `.rddf/state/.cross-repo-audit.jsonl` 经端到端测...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

## Verification

```bash
./test.sh --full --regression
```

Exit code 0 = all pass → proceed to commit.
