# verifier-archive-gate-clarification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use skill_use("execute") to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 `docs/adr/ADR-0035-verifier-archive-gate-boundary.md` 显式记录 rdd-verifier 与 archive_gate_check 双轨设计,定义 4 个 scenario 边界 + STRICT_AC_GATE escalation token 语义。

**Architecture:** 直接添加 ADR-0035 markdown 文件,在 _lib/archive.sh::archive_gate_check 顶部加注释引用 ADR-0035 §1。bats integration test 验证 ADR 文件存在 + 文档关键词 ("verifier-archive-gate boundary" / "STRICT_AC_GATE")。

**Tech Stack:** Markdown (ADR), bash (archive.sh comment), bats (integration)

---

## File Structure

### Production Code

| File | Responsibility |
|---|---|
| `docs/adr/ADR-0035-verifier-archive-gate-boundary.md` | 新 ADR:定义 4 个 scenario 边界 + STRICT_AC_GATE 语义 |
| `_lib/archive.sh` | archive_gate_check 函数顶部注释引用 ADR-0035 §1 |
| `README.md` (docs/adr/) | 注释 (auto-generated by adr-index-auto-sync) |

### Tests

| File | Responsibility |
|---|---|
| `tests/integration/test_verifier_archive_gate.bats` | 验证 ADR-0035 存在 + archive.sh 注释引用 + 4 个 scenario 关键词存在 |

---

### Task 1: 创建 ADR-0035

**Files:**
- Create: `docs/adr/ADR-0035-verifier-archive-gate-boundary.md`

- [ ] **Step 1: 写 ADR-0035 内容**

```markdown
# ADR-0035: rdd-verifier ↔ archive_gate_check 双轨设计边界

> **状态**: 已采纳
> **日期**: 2026-08-28
> **决策者**: sisyphus
> **关联**: ADR-0034 (rdd-verifier 阶段架构), ADR-0027 (L2 上报)

## 背景

ADR-0034 §3 定义 rdd-verifier 为"条件必经"(默认必走,`SKIP_RDD_VERIFIER=yes` 跳过)。同时 §5 让 `archive_gate_check` 接受 archive 唯一条件:`verification.state ∈ (passed, bypassed) AND archive_ready == true`。

ADR-0034 §"中立"段保留 archive_gate_check 内嵌 ac-verifier 调用作为"fallback"——"用户绕过 rdd-verifier 直接 archive 时仍守门,且 fallback 写结构化 cache"。

这是**双轨设计**:
- **正常路径**: `rdd-verifier` → cache → `archive_gate_check` 读 cache 接受
- **fallback 路径**: 跳过 verifier → `archive_gate_check` 内嵌 `ac-verifier` → 写 cache (`ran_by=archive_gate_check`) → 接受

## 4 个 Scenario 边界

### 场景 1（标准路径）

change 走 rdd-verifier 验证 → cache hit → archive 通过。

- `verification.state = passed`
- `verification.archive_ready = true`
- `verdict_sha` 绑定到当前 commit SHA

### 场景 2（fallback 路径）

change 跳过 rdd-verifier → archive_gate 内嵌 ac-verifier → cache 写 `ran_by=archive_gate_check` → archive 通过。

- `verification.state = bypassed`
- `verification.bypass_source = 'SKIP_RDD_VERIFIER'`
- `verification.archive_ready = true`

### 场景 3（halted）

change 在 rdd-verifier 中触发 max_loops → archive 阻断 (exit 4)。

- `verification.state = halted`
- `verification.archive_ready = false`
- `route = halted` → 人工 review

### 场景 4（on-main mode — 紧急 hotfix）

change 走 `tools/archive_on_main.sh --confirm-main`(与 verifier 无关, USAGE §"On-main Mode")。

- 跳过 verification 对象 (compat 模式)
- 仅 emergency, 不推荐

## 权衡:绕过 verifier

| 选项 | Token 节省 | 失败回环能力 |
|------|-----------|-------------|
| 走 rdd-verifier (场景 1) | 高 (批量 4+ change 时明显) | ✅ 完整 |
| 走 archive_gate fallback (场景 2) | 中 (省 rdd-verifier 阶段) | ⚠️ 单 change 失败回 plan/ship |
| 走 on-main mode (场景 4) | 最高 | ❌ 无 |

**建议**: 大型 release 走场景 1; 单 change hotfix 可走场景 2; 紧急 hotfix 走场景 4。

## STRICT_AC_GATE 升级

`STRICT_AC_GATE=yes` 强制 `SKIP_RDD_VERIFIER=yes` 失效 (fatal error, refuses to archive):

```
🚫 STRICT_AC_GATE active; SKIP_RDD_VERIFIER bypass refused
```

设置方法:
```bash
export STRICT_AC_GATE=yes   # CI / strict mode
unset STRICT_AC_GATE         # 默认 (warning only)
```

## 后续 follow-up

- 弃用 on-main mode (留作 v3.1 follow-up)
- rdd-verifier cache 加 SHA 指纹 (已有 per ADR-0034)

## 参考

- ADR-0034 §3 (rdd-verifier 阶段架构)
- ADR-0034 §5 (archive_gate_check acceptance 条件)
- ADR-0027 (L2 上报契约)
- docs/adr/ADR-0027-supersede.md (历史 ADR-0027 拆分)
```

- [ ] **Step 2: 验证 ADR 文件存在**

Run: `test -f docs/adr/ADR-0035-verifier-archive-gate-boundary.md && echo OK`
Expected: OK

- [ ] **Step 3: Defer commit**

---

### Task 2: 文档 ADR 索引段自动更新 (generator 重新跑)

**Files:**
- Modify: `docs/adr/README.md` (auto-generated by adr-index-auto-sync generator)

- [ ] **Step 1: 运行 generator 更新 README**

Run: `python3 -c "
import sys
sys.path.insert(0, '_lib')
from adr_index_generator import scan_adrs, render_table
from pathlib import Path

adrs = scan_adrs(Path('docs/adr'))
table = render_table(adrs)

import re
readme = Path('docs/adr/README.md').read_text()
pattern = re.compile(r'<!-- ADR_INDEX_START -->\\n.*?<!-- ADR_INDEX_END -->', re.DOTALL)
new_table_block = f'<!-- ADR_INDEX_START -->\\n{table}<!-- ADR_INDEX_END -->'
new_readme = pattern.sub(new_table_block, readme, count=1)
Path('docs/adr/README.md').write_text(new_readme)
print('README updated')
"`

- [ ] **Step 2: 验证 ADR-0035 出现在 README 表格中**

Run: `grep "ADR-0035" docs/adr/README.md`
Expected: 输出 ≥ 1 行

- [ ] **Step 3: Defer commit**

---

### Task 3: _lib/archive.sh 顶部注释引用 ADR-0035

**Files:**
- Modify: `_lib/archive.sh`

- [ ] **Step 1: 找到 archive_gate_check 函数定义**

Run: `grep -n "archive_gate_check()" _lib/archive.sh | head`
Expected: 输出函数定义所在行

- [ ] **Step 2: 在函数顶部加注释引用 ADR-0035**

用 `edit` 工具,在 `archive_gate_check()` 函数体开头(第一行命令之前)插入:

```bash
# ADR-0035 §1: verifier-archive-gate boundary
# This function implements scenario 2 (fallback) when rdd-verifier
# is skipped (SKIP_RDD_VERIFIER=yes). For scenario 1 (standard),
# rdd-verifier already wrote the verdict cache. Scenario 3 (halted)
# is blocked upstream by rdd-verifier max_loops guard.
# See docs/adr/ADR-0035-verifier-archive-gate-boundary.md for full semantics.
```

- [ ] **Step 3: 验证注释存在**

Run: `grep -c "ADR-0035" _lib/archive.sh`
Expected: ≥ 1

- [ ] **Step 4: Defer commit**

---

### Task 4: 创建 bats integration test

**Files:**
- Create: `tests/integration/test_verifier_archive_gate.bats`

- [ ] **Step 1: 写 bats test**

```bash
# tests/integration/test_verifier_archive_gate.bats
load 'test_helper'

setup() {
    REPO_ROOT="${BATS_TEST_DIRNAME}/../.."
    cd "$REPO_ROOT"
}

@test "verifier-archive-gate: ADR-0035 file exists" {
    [ -f "docs/adr/ADR-0035-verifier-archive-gate-boundary.md" ]
}

@test "verifier-archive-gate: ADR-0035 documents all 4 scenarios" {
    grep -q "场景 1" docs/adr/ADR-0035-verifier-archive-gate-boundary.md
    grep -q "场景 2" docs/adr/ADR-0035-verifier-archive-gate-boundary.md
    grep -q "场景 3" docs/adr/ADR-0035-verifier-archive-gate-boundary.md
    grep -q "场景 4" docs/adr/ADR-0035-verifier-archive-gate-boundary.md
}

@test "verifier-archive-gate: ADR-0035 documents STRICT_AC_GATE escalation" {
    grep -q "STRICT_AC_GATE" docs/adr/ADR-0035-verifier-archive-gate-boundary.md
}

@test "verifier-archive-gate: archive.sh references ADR-0035" {
    grep -q "ADR-0035" _lib/archive.sh
}

@test "verifier-archive-gate: README.md ADR index includes ADR-0035" {
    grep -q "ADR-0035" docs/adr/README.md
}
```

- [ ] **Step 2: 运行 bats**

Run: `bats tests/integration/test_verifier_archive_gate.bats`
Expected: 5 pass

- [ ] **Step 3: Defer commit**

---

### Task 5: 跑全量回归 + 归档前 sanity check

**Files:**
- 无 (验证步骤)

- [ ] **Step 1: 运行 pytest + bats**

Run: `./test.sh --quick`
Expected: ALL PASSED

- [ ] **Step 2: archive 前 full regression**

Run: `./test.sh --full --regression`
Expected: 0 exit code, no new failures

- [ ] **Step 3: Defer commit**

---

## Self-Review Checklist

- [ ] Spec 覆盖: 5 acceptance items 对应 Task 1-4
  - Task 1: ADR-0035 创建 ✅
  - Task 2: README 更新 ✅
  - Task 3: archive.sh 注释 ✅
  - Task 4: bats CI ✅
  - Task 5: 全量回归 ✅
- [ ] 占位符扫描: 0 个 TBD
- [ ] 类型一致性: ADR 章节 (背景/4 scenario/权衡/STRICT_AC_GATE/后续/参考) 完整