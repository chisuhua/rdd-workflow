# fix-cli-routing-cross-repo-commands — Implementation Plan

> Generated by guide-ship Phase 1
> Change: fix-cli-routing-cross-repo-commands
> Mode: worktree

## Goal

Implement fix-cli-routing-cross-repo-commands per proposal AC + design decisions.

## Architecture

### Decision 1: Approach from proposal 关键场景

The proposal's 5 key scenarios drive the implementation:

## Tech Stack

- bash (approve_proposal.sh, gate scripts)
- Python 3.11+ (_lib modules, cross_repo_audit.py, iteration/store.py)
- bats (integration tests)
- pytest (unit tests)
- openspec CLI v1.4.1

## Tasks

### Task 1: `_lib/cli/__init__.py::_ROUTES` 新增 3 条 entry（sync-hub / watch-hub / deps.cross-repo）

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for `_lib/cli/__init__.py::_ROUTES` 新增 3 条 entry（sync-hub / watc...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 2: `tests/integration/test_rddf_cli_routing.bats` 新增, 3 个 case 全绿

**Files:**
- Test: tests/integration/test_*.bats

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for `tests/integration/test_rddf_cli_routing.bats` 新增, 3 个 case ...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 3: `tests/unit/test_deps_cmd.py` 新增 cross-repo 路由分发 3 个 case

**Files:**
- Test: tests/unit/test_*.py

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for `tests/unit/test_deps_cmd.py` 新增 cross-repo 路由分发 3 个 case...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 4: 实跑 `rddf sync-hub --help` 返回帮助（不再是 "unknown command"）

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for 实跑 `rddf sync-hub --help` 返回帮助（不再是 "unknown command"）...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 5: 实跑 `rddf watch-hub --help` 返回帮助

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for 实跑 `rddf watch-hub --help` 返回帮助...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 6: 实跑 `rddf deps cross-repo --help` 返回帮助

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for 实跑 `rddf deps cross-repo --help` 返回帮助...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 7: **既有回归**: `tests/integration/test_cli_routing.bats` 既有 22 个命令 case 全绿

**Files:**
- Test: tests/integration/test_*.bats

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for **既有回归**: `tests/integration/test_cli_routing.bats` 既有 22 个命...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 8: **既有回归**: `tests/unit/test_deps_cmd.py` 全绿

**Files:**
- Test: tests/unit/test_*.py

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for **既有回归**: `tests/unit/test_deps_cmd.py` 全绿...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 9: **既有回归**: `./test.sh --full --regression` 通过

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for **既有回归**: `./test.sh --full --regression` 通过...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 10: **README 同步**: README §跨项目协同 章节示例命令更新为 `rddf sync-hub` / `rddf watch-hub` / `rddf deps cross-repo`

**Files:**
- Modify: README.md

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for **README 同步**: README §跨项目协同 章节示例命令更新为 `rddf sync-hub` / `rd...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

### Task 11: **GitHub Actions 同步**: `.github/workflows/contract-check.yml` 若引用旧命令路径需更新

**Files:**

**Steps (TDD 5-step):**

- [ ] 1. Write failing test for **GitHub Actions 同步**: `.github/workflows/contract-check.yml...
- [ ] 2. Verify test fails (RED)
- [ ] 3. Implement minimal code to pass
- [ ] 4. Verify test passes (GREEN)
- [ ] 5. Refactor if needed + update tasks.md checkbox

## Verification

```bash
./test.sh --full --regression
```

Exit code 0 = all pass → proceed to commit.
