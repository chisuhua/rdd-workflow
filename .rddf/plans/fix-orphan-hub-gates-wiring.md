# fix-orphan-hub-gates-wiring — Implementation Plan

> Generated by guide-ship Phase 1
> Change: fix-orphan-hub-gates-wiring
> Mode: worktree

## Goal

Implement fix-orphan-hub-gates-wiring per proposal AC + design decisions.

## Architecture

### Decision 1: Approach from proposal 关键场景

The proposal's 5 key scenarios drive the implementation:

## Tech Stack

- bash (approve_proposal.sh, gate scripts)
- Python 3.11+ (_lib modules, cross_repo_audit.py, iteration/store.py)
- bats (integration tests)
- pytest (unit tests)
- openspec CLI v1.4.1

## Tasks

### Task 1: `skills/guide-design/SKILL.md` Phase 4 `check_design_done_gate()` 末尾追加 2 个新 check

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `skills/guide-design/SKILL.md` Phase 4 `check_design_done_ga...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 2: `tests/integration/test_design_done_hub_gates.bats` 新增, 4 个场景全绿

**Files:**
- Test: tests/integration/test_*.bats

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `tests/integration/test_design_done_hub_gates.bats` 新增, 4 个场...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 3: 默认 + hub pending → exit 1

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for 默认 + hub pending → exit 1...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 4: 默认 + cross_repo_audit 含未批准 → exit 1

**Files:**
- Modify: skills/_lib/cross_repo_audit.py

**Steps (TDD 5-step):**

- [x] 1. Write failing test for 默认 + cross_repo_audit 含未批准 → exit 1...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 5: `SKIP_HUB_CHECK=true` → exit 0 (含 audit)

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `SKIP_HUB_CHECK=true` → exit 0 (含 audit)...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 6: 空 audit + 空 pending → exit 0 默认通过

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for 空 audit + 空 pending → exit 0 默认通过...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 7: `rdd-doctor` 新增 `--check orphan-gates` 巡检模式, 当 orphan 函数被检测到时 CRITICAL 报告

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `rdd-doctor` 新增 `--check orphan-gates` 巡检模式, 当 orphan 函数被检测到...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 8: `tests/unit/test_design_done_gate.py` 全绿（既有）

**Files:**
- Test: tests/unit/test_*.py

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `tests/unit/test_design_done_gate.py` 全绿（既有）...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 9: `tests/unit/test_rdd_doctor.py` 新增 orphan-gates 单测覆盖

**Files:**
- Test: tests/unit/test_*.py

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `tests/unit/test_rdd_doctor.py` 新增 orphan-gates 单测覆盖...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 10: `README.md` §"紧急跳过 `SKIP_HUB_CHECK=true`" 章节明确"默认 OFF, 紧急时 ON"语义

**Files:**
- Modify: README.md

**Steps (TDD 5-step):**

- [x] 1. Write failing test for `README.md` §"紧急跳过 `SKIP_HUB_CHECK=true`" 章节明确"默认 OFF, 紧急时 O...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 11: **既有回归**: `./test.sh --full --regression` 通过

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **既有回归**: `./test.sh --full --regression` 通过...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 12: **审计 trail**: `git log --grep='fix-orphan-hub-gates'` 含清晰 conventional commit

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **审计 trail**: `git log --grep='fix-orphan-hub-gates'` 含清晰 co...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

### Task 13: **依赖记录**: proposal-suggestions.md 表头注明 "阻塞: fix-adr-0031-safety-gate-substantiation"

**Files:**

**Steps (TDD 5-step):**

- [x] 1. Write failing test for **依赖记录**: proposal-suggestions.md 表头注明 "阻塞: fix-adr-0031-saf...
- [x] 2. Verify test fails (RED)
- [x] 3. Implement minimal code to pass
- [x] 4. Verify test passes (GREEN)
- [x] 5. Refactor if needed + update tasks.md checkbox

## Verification

```bash
./test.sh --full --regression
```

Exit code 0 = all pass → proceed to commit.
