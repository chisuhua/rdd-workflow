# add-archive-post-commit-hook-and-force-flag — Tasks

> Schema: spec-driven
> Auto-generated by guide-plan from improvements/add-archive-post-commit-hook-and-force-flag.md
> See: `proposal.md` (motivation/scope) + `design.md` (technical decisions).

## 1. Implementation

### A. git post-commit hook (the "唯一漏洞" per proposal §症状 1)

- [x] A.1 `.git-hooks/post-commit` (POSIX sh) — 检测 commit diff 含 `openspec/changes/archive/<date>-<name>/` 路径，调用 `sync_iteration_after_archive <name>`
- [x] A.2 hook stdout 输出简洁 ≤3 行（"✅ archive hook synced: <name>" 或 "⚠️ archive hook failed for <name>: <err>"）
- [x] A.3 hook 任何失败 `exit 0`（永不阻断 commit）
- [x] A.4 hook 在非 archive commit 上触发 → `exit 0`，无副作用，无 stdout 输出
- [x] A.5 hook 用严格 `^\d{4}-\d{2}-\d{2}-.+$` 正则匹配日期前缀，提取 name 部分
- [x] A.6 hook 在 macOS + Linux 双平台可工作（POSIX sh 兼容, 不依赖 bashisms）

### B. install script (idempotent)

- [x] B.1 `scripts/install-archive-hooks.sh` — 一键安装 `core.hooksPath` + `.git-hooks/post-commit`
- [x] B.2 install 重复执行结果一致（幂等）
- [x] B.3 install stdout 简洁，已安装时打印 `✓ hooks already installed`

### C. `rddf archive-sync <name>` 一次性数据 reconcile 工具

- [x] C.1 `skills/_lib/cli/archive_sync_cmd.py` — 新增 `rddf archive-sync <name1> [name2 ...]` 子命令
- [x] C.2 复用 `sync_iteration_after_archive` helper (from #1)
- [x] C.3 支持批量：单次调多个 name
- [x] C.4 `skills/_lib/cli/__init__.py` 注册 `archive-sync` 路由

### D. Deferred (out of scope for this P0)

- [ ] D.1 `--force` / `--no-validate` CLI flag for `rddf status --archive` — requires `--archive` mode to exist first (status_cmd.py Mode C not yet implemented; independent follow-up)
- [ ] D.2 `SKIP_ARCHIVE_GATE` / `SKIP_DELTA_VALIDATION` env var wiring in archive.sh

### E. Tests

- [x] E.1 `tests/integration/test_archive_post_commit_hook.bats` — 7 case: archive path detected / non-archive commit ignored / helper fail tolerated / idempotent / date prefix / POSIX compliance / install idempotency
- [x] E.2 `tests/integration/test_archive_sync_cli.bats` — 6 case: single name / missing name / no args / --all with candidates / --all no candidates / multiple names

## 2. Verification

- [x] 2.1 Run all existing bats + pytest tests to confirm no regression
- [ ] 2.2 Commit working tree in worktree with conventional commit message
