# Per-change metadata for verifier-v2-hardening
# Read by propose_finalize_change / guide-plan / rdd-builder for phase assignment

phase: phase-5
category: refactor
priority: P0

# Manual dependencies (ADR-0022): v2.0 closure fix + follow-up scheduling.
# - inline-ac-verifier-into-rdd-verifier: source of the v2.0 protocol that this
#   change tightens (validated completeness, schema strictness, atomic writes,
#   schema_version fail-closed). v2.0 cache + audit + loop state contracts
#   inherited unchanged.
# - This change should land BEFORE remove-ac-verifier-completely closes the
#   shim window (P1 verdict completeness is a precondition for safely
#   removing the deprecation fallback path).
manual_deps:
  - inline-ac-verifier-into-rdd-verifier

# Manual blocks (changes that require verifier-v2-hardening to land first).
# - remove-ac-verifier-completely: depends on verdict completeness, schema
#   strictness, and atomic writes being in place before deprecation shim
#   closes.
# - planner-deprecation-aware-scaffolding: subsumed by Phase 7 of this
#   change (scan_deprecated_skills helper exposed in _lib/planner_*.py).
manual_blocks:
  - remove-ac-verifier-completely