# Tasks: fix-scan-state-binding

> Auto-generated by guide-ship. Mark `- [ ]` -> `- [x]` as each task completes.

- [ ] Task 1: Fix scan-state.sh owner syntax and split heartbeat GC helper
  - Repair the broken `local owner="${OPENCODE_SESSION_ID:-$(hostname -s)_$$}"` assignment in `skills/guide/scripts/scan-state.sh`
  - Extract `check_heartbeat_timeouts()` into a separate shell helper and call it from `scan_session_binding()` before binding lookup
- [ ] Task 2: Wire owner-based current-session detection into dashboard collect()
  - In `skills/_lib/dashboard/__init__.py::collect()`, prefer `owner_opencode_session_id == OPENCODE_SESSION_ID` when marking `is_current`
  - Fall back to most-recent-active behavior when no owner match exists
- [ ] Task 3: Add bats regression test for binding scan and heartbeat flow
  - Verify `scan_session_binding` returns current binding when owner matches
  - Verify the owner fallback string stays single-line when `OPENCODE_SESSION_ID` is unset
  - Verify missing `sessions.json` still returns success with no binding lines
- [ ] Task 4: Extend dashboard unit coverage for owner-based current marking
  - Verify `collect()` marks the owner-matching session as current when `OPENCODE_SESSION_ID` is set
  - Verify `collect()` falls back to the most recent active session when `OPENCODE_SESSION_ID` is unset
- [ ] Task 5: Run bats and pytest, then verify diagnostics are clean
  - Run the new bats regression plus targeted pytest for dashboard collection
  - Confirm changed files have no LSP diagnostics errors
