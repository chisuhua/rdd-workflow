# Per-change metadata for inline-ac-verifier-into-rdd-verifier
# Read by propose_finalize_change / guide-plan / rdd-builder for phase assignment

phase: phase-5
category: refactor
priority: P1

# Manual dependencies (ADR-0022): this change supersedes ac-verifier
manual_deps:
  - fix-rdd-verifier-lifecycle-dashboard  # established verifier loop state + cache schema v2
  - add-full-regression-gate              # provides the ./test.sh --full --regression baseline

# Manual blocks (changes that must wait for this one)
manual_blocks:
  - rdd-verifier-v2-rollout        # depends on deprecation window closing
  - remove-ac-verifier-completely  # follow-up that actually deletes ac-verifier scripts
