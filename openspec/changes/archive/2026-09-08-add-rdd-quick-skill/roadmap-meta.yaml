# Per-change metadata for add-rdd-quick-skill
# Read by propose_finalize_change / guide-plan / rdd-builder for phase assignment

phase: phase-2
category: arch-design
priority: P1

# Roadmap theme binding (per roadmap-proposal-guidance).
# Matches phase-2 theme "编排能力完善" (ADR-0004 loop engine + ADR-0011 step pipeline)
# — rdd-quick adds a NEW orchestration path rather than refining an existing phase.
theme: 编排能力完善

# Manual dependencies (ADR-0022): none — implementation is fully additive.
# rdd-quick reuses two mature contracts as read-only references (no code coupling):
# - rdd-verifier v2.0 VERDICT_ITEM_SCHEMA (ADR-0045) for the P3 verdict format
# - rdd-doctor plan_tdd_check.py TDD 5-marker contract for the plan file
# Neither is modified, so no ordering constraint is required.
manual_deps: []

# Manual blocks: none. guide-ship-quick-finish (P2, unapproved) targets a
# DIFFERENT scenario (existing change nearly done → skip to archive) and is
# explicitly NOT merged or modified by this change. The two coexist.
manual_blocks: []
