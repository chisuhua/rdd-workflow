# fix-rddf-status-corrupt-message — Tasks

> Schema: spec-driven
> Auto-generated by guide-plan from improvements/fix-rddf-status-corrupt-message.md
> See: `proposal.md` (motivation/scope) + `design.md` (technical decisions).

## 1. Implementation

- [x] 1.1 文件缺失 → `(None, None)`
- [x] 1.2 `JSONDecodeError`/`OSError` → `(None, f"invalid JSON: {e}")`
- [x] 1.3 `ValidationError` → `(None, f"schema validation failed at {list(e.absolute_path)}: {e.message}")` — direct `Draft7Validator.iter_errors()` to preserve path (worktree's `_validate` discards path when re-raising)
- [x] 1.4 成功 → `(data, None)`
- [x] 1.5 `_read_unlocked` 改为委托 `_read_unlocked_verbose` 并丢弃 error (一行改动, 零行为变化)
- [x] 1.6 `_mode_a` (L303-306) 和 `_mode_b` (L171-174) 切换到 `read_iteration_or_corrupt`
- [x] 1.7 文件缺失 (`error is None` 且文件不存在) → 保留现有 "not found" 消息, 并附加文件路径 `.rddf/state/iteration.json`
- [x] 1.8 文件存在但损坏 → 新消息 (见"关键场景"), **不显示** `skill_use("propose", ...)` 提示 — via shared `_render_iteration_read_error()` helper
- [x] 1.9 返回码: corrupt 时 `_mode_a` 返回 1 (与 `_mode_b` 现状对齐; 当前 corrupt 走 "not found" 分支 `_mode_a` 返回 0 是隐藏 bug — CLI 静默成功)
- [x] 1.10 `tests/unit/test_state_reader_corrupt.py`: 5 case (missing / invalid JSON / schema-invalid / valid / readonly-no-backups) — tmpdir 构造 iteration.json
- [x] 1.11 `tests/unit/test_iteration_store_verbose.py`: 3 case 锁定 `_read_unlocked_verbose` 返回形状
- [x] 1.12 `tests/integration/test_status_corrupt_message.bats`: 6 case 端到端 `rddf status` 在 corrupt iteration.json 下的输出断言 (含 "不得出现 propose 提示" 反向断言)
- [x] 1.13 `skills/feature/scripts/feature_cli.py` corruption check (提案要求场景 4)
## 2. Verification

- [x] 2.1 Run all existing bats + pytest tests to confirm no regression
- [ ] 2.2 Commit working tree in worktree with conventional commit message
