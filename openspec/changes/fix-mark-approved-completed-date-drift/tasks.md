## 1. 幂等日期保留

- [x] 1.1 在 `state.sh::mark_approved_completed` 幂等分支中，命中"已实施"区条目时从现有行提取原完成日期并复用（而非无条件使用当前日期）
- [x] 1.2 验证幂等调用后条目保持原日期（如 2026-07-23 → 仍为 2026-07-23）

## 2. 行为保持

- [x] 2.1 验证首次归档（从已批准区移入）时日期 = 调用当天（行为不变）
- [ ] 2.2 验证 change 不在文件中时返回 1 不修改文件（行为不变）

## 3. 测试

- [x] 3.1 新增幂等日期保留测试：构造已实施区含原日期的 fixture，断言幂等调用后日期不变
- [x] 3.2 运行新增测试通过，`bats tests/` 与 `python3 -m pytest tests/unit/ -q` 全量回归通过
