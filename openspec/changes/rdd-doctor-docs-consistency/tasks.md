# rdd-doctor-docs-consistency — Tasks

> **Generated by guide-plan Phase 2.5 fill** (design-pre-created path)
> Schema: spec-driven | Artifact: tasks

## Implementation Tasks

Detailed TDD 5-step tasks will be generated by `guide-ship` → `rdd-workflow-writing-plans`
into `.rddf/plans/rdd-doctor-docs-consistency.md` (per `docs/superpowers/specs/2026-08-05-guide-ship-execution-contract.md`).

The high-level checklist below mirrors the proposal.md ## Acceptance section; ship 阶段会展开为子任务。

- [ ] Implement the change per `proposal.md` ## What Changes (In Scope)
- [ ] Verify all `proposal.md` ## Acceptance checkboxes are satisfied
- [ ] Run `./test.sh --quick` and ensure no new failures
- [ ] Update `proposal-approved.md` if change status moves (archive hooks do this automatically)

## Dependencies (prefilled from openspec instructions)

- [x] design (Technical design decisions and implementation approach)
- [x] proposal (Why + What Changes)

## Notes

- Execute reads this file and updates progress checkboxes ([ ] → [x]) during implementation.
- archive gate checks: this file's checkboxes must all be [x] for tasks-completion gate
  (per `enforce-tasks-completion-before-archive` improvement).
